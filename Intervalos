N = int(input())
contador = 1
dentro = 0
fora = 0

while (contador <= N):
    X = int(input())
    if ((X >= 10) & (X <= 20)):
        dentro = dentro + 1
    else: fora = fora + 1
    contador = contador + 1

print("{} in\n{} out".format(dentro, fora))
